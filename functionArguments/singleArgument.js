const wallpainter = function(item) {
    console.log("The wall has been painted " + item);
};

wallpainter("green");
wallpainter("purple");
wallpainter();

// If we call a function without an argument the answer is undefined